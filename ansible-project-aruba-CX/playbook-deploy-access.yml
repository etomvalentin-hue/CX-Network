---
- name: Deploy Access Switch Configuration
  hosts: access
  gather_facts: true
  collections:
    - arubanetworks.aos_switch

    #- "{{ playbook_dir }}/inventory/production/group_vars/vault.yml"
    
  vars_files:
    - inventory/production/group_vars/password
    
  vars: 
    ansible_user: admin
    ansible_password: admin2023
    
  roles:
    - access_switch